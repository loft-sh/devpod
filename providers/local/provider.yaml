name: local
version: 0.0.1
description: |-
  DevPod on Local Machine
agent:
  path: devpod
exec:
  command: |-
    ${DEVPOD} helper sh -c "${COMMAND}"
  delete: |-
    ${DEVPOD} agent cleanup --workspace-info "${WORKSPACE_INFO}"
